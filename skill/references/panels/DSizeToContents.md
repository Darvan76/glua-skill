# DSizeToContents

---

**Hereda de:** `Panel`

---

A helper panel that will automatically resize itself to fit all its children using Panel:SizeToChildren.

---


## Miembros (4)


---

### `DSizeToContents:GetSizeX` `[client/menu]`

Returns whether the DSizeToContents panel should size to contents horizontally.

**Argumentos:**

*(sin argumentos)*

**Retorna:**

- `boolean` — Whether the panel should size to contents horizontally.

---

### `DSizeToContents:GetSizeY` `[client/menu]`

Returns whether the DSizeToContents panel should size to contents vertically.

**Argumentos:**

*(sin argumentos)*

**Retorna:**

- `boolean` — Whether the panel should size to contents vertically.

---

### `DSizeToContents:SetSizeX` `[client/menu]`

Sets whether the DSizeToContents panel should size to contents horizontally. This is `true` by default.

**Argumentos:**

- `boolean sizeX` — Whether the panel should size to contents horizontally.

**Retorna:**

*(sin retorno)*

---

### `DSizeToContents:SetSizeY` `[client/menu]`

Sets whether the DSizeToContents panel should size to contents vertically. This is `true` by default.

**Argumentos:**

- `boolean sizeY` — Whether the panel should size to contents vertically.

**Retorna:**

*(sin retorno)*